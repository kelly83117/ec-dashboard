# PChome 淨利表區塊 — 開發交接

給 Claude Code｜來源：2026/09/08、09/16 兩份對帳明細匯出 + 對帳單頁面

**這份文件裡的資料結構細節都是從實際檔案挖出來的，repo 裡看不到。開工前先讀完。**

---

## 0. 開工前的兩件事

### TEST_NOWRITE（最重要）

#### 🔴 測前必做：先單獨確認 guard 已生效（放在所有本機測試步驟的最前面）

**任何會寫入的本機測試之前**，先在 F12 Console **單獨執行**這一行、看它回 `true`：

```js
window.__TEST_NOWRITE_ON === true
```

- 回 `true` → guard 生效，可以測。
- 回 `false` 或 `undefined` → **立刻停手**，guard 沒貼（或沒生效），先照下面把防護碼貼進 `js/firebase.js`、重載、看到紅字，再回來重跑這行確認。

⚠ **這個檢查一定要「獨立執行」**：先單獨貼這一行、確認 `true`，**才能**接著做會寫入的操作。**不可以**把它跟寫入動作（存對帳、按同步、上傳套用…）寫在同一段腳本／同一次貼上——合在一起就等於「沒把關」：真值是 false 時，寫入照樣先跑了。

⚠ **最容易漏的時機＝「合併完 → 下次測試」之間**。guard 刻意不 commit，所以**每次合併都會把它剝掉**；下次要測時 `firebase.js` 裡沒有 guard、`__TEST_NOWRITE_ON` 是 `undefined`。
根因是**拆除與貼回不對稱**——「測完拆除」下面有明確步驟、清單、順序；「下次測前貼回」以前沒有任何步驟，靠人記，所以常漏。這一段就是補上那個不對稱的缺口：**拆除有步驟、貼回也必須有一步（就是先跑上面這行）**。

---

本專案前端直連正式 Firestore，**沒有測試環境**。`js/app.js` 把 `Store.set` patch 成「寫入即同步正式庫」，所以**在本機起 server、光是載入頁面就會寫公司資料**。

要在本機測試，必須先把 TEST_NOWRITE 防護碼貼進 `js/firebase.js`：

- 位置：`window.dispatchEvent(new Event('cloudStoreReady'));` **之前**（此時 17 個 `__cloud*` 物件都已建好，掃得到）
- 用這個錨點找，不要記行號
- 貼 F12 主控台無效（patch 閉包已捕獲原物件）

**開 App 第一件事看 Console，要有紅字「TEST_NOWRITE v2 已啟用：17 個物件 / 25 個寫入方法已停用」**，才能測。

- **判斷標準是物件數**：目前 **17 物件 / 25 方法**（2026-09-16 實測；`table` 57 條屬性＝25 攔截＋31 放行讀取＋1 非函式，另 forKey 包裝 1 條）。
- ⚠ **物件數會隨新增 cloud 物件而變，當場重數**（`grep -nE "^\s*(window\.)?__cloud[A-Za-z0-9_]*\s*=" js/firebase.js`）。**數字對不上就停下來查是哪個物件沒被掃到，不要因為「比預期多」就當作沒事**——少於預期＝有寫入路徑沒被擋（保護有洞）；多於預期＝有新物件，要確認它的寫入方法也被 no-op 且已補進 MUST。
- ⚠ **guard 的 `MUST` 明列清單要跟著新增的 cloud 物件一起維護**：MUST 是 fail-loud（動態掃描抓不到「應該存在卻不存在」，靠 MUST assert throw）。新增 cloud 物件時，把它的寫入方法同步補進 MUST，否則新物件沒有 fail-loud 保護。
- 動態掃描下限已設「至少 17」，低於就 throw（原本「至少 6」等於沒有下限）。
- ⚠ **與唯讀角色防護共存**：`js/app.js` 的 `__installReadonlyCloudGuard` 會在 `cloudStoreReady` 後再包一層（只在 role='view' 時攔）。因 TEST_NOWRITE 先跑、它包到的是 no-op，所以**即使不是 view 角色寫入仍被擋**（可打 `window.__TEST_NOWRITE_HITS` 看攔截紀錄佐證）。用 `.toString()` 看方法會看到外層唯讀 wrapper，別誤判成沒生效。

防護碼本體（貼進 firebase.js 上述位置；commit 前必刪）：

```js
/* ===== TEST_NOWRITE v2（本機測試用 — commit 前必須手動刪除）===== */
(function () {
  'use strict';
  var H = location.hostname;
  if (H !== 'localhost' && H !== '127.0.0.1' && H !== '::1') {
    throw new Error('[TEST_NOWRITE] 非本機環境（' + H + '），拒絕啟動。這段程式碼絕對不能進 git。');
  }
  if (window.__TEST_NOWRITE_ON) throw new Error('[TEST_NOWRITE] 偵測到重複套用，請檢查是不是貼了兩份。');
  var READ_OK = ['getDoc', 'subscribe', 'getDocForShop', 'subscribeShop'];
  // 全 17 個雲端物件的寫入方法都要明列（fail-loud）。新增 cloud 物件時同步補進來。
  var MUST = [
    ['__cloudStore','setField'],['__cloudStore','removeField'],['__cloudStore','removeFields'],
    ['__cloudProfit','setField'],['__cloudProfit','removeFields'],
    ['__cloudProfitCol','setReport'],
    ['__cloudInsight','setField'],['__cloudInsight','removeField'],['__cloudInsight','removeFields'],
    ['__cloudInsightByShop','forKey'],
    ['__cloudMomo','setShop'],['__cloudReconcile','setMonth'],['__cloudS1103','setPeriod'],
    ['__cloudStock','setSnapshot'],['__cloudMoPlusOrigins','setSrc'],['__cloudE001','setSrc'],
    ['__cloudCostByOrigin','set'],
    ['__cloudAff','set'],['__cloudAff','removeDoc'],
    ['__cloudTaskImage','setImage'],['__cloudTaskImage','removeImage'],
    ['__cloudCoupang','setReport'],['__cloudCoupangMsf','setSrc'],['__cloudCoupangNote','setNote']
  ];
  var table = [], hits = []; window.__TEST_NOWRITE_HITS = hits;
  function makeNoop(path){ return function(){ var rec={時間:new Date().toLocaleTimeString(),呼叫:path,參數:Array.prototype.slice.call(arguments)}; hits.push(rec); console.warn('%c🛑 已攔截寫入 → '+path,'color:#ef4444;font-weight:700',rec.參數); return Promise.resolve(); }; }
  var names = Object.keys(window).filter(function(k){ return k.indexOf('__cloud')===0 && window[k] && typeof window[k]==='object'; });
  if (names.length < 17) throw new Error('[TEST_NOWRITE] 只找到 '+names.length+' 個 __cloud 物件（預期至少 17）：'+names.join(', ')+'。少於預期＝有物件沒被掃到（保護有洞）→ 停下來查，不要硬跑。');
  MUST.forEach(function(p){ var o=window[p[0]]; if(!o||typeof o[p[1]]!=='function') throw new Error('[TEST_NOWRITE] 找不到 '+p[0]+'.'+p[1]+'，firebase.js 已被改過。請重新盤點後再測試。'); });
  var byShop = window.__cloudInsightByShop, origForKey = byShop.forKey;
  byShop.forKey = function(key){ var r=origForKey.call(byShop,key); if(!r) return r; var safe={}; Object.keys(r).forEach(function(k){ safe[k]=(typeof r[k]==='function'&&READ_OK.indexOf(k)<0)?makeNoop('__cloudInsightByShop.forKey().'+k):r[k]; }); return safe; };
  table.push({物件:'__cloudInsightByShop',屬性:'forKey() 的回傳值',處置:'🛑 包裝後 no-op'});
  names.forEach(function(n){ var o=window[n]; Object.keys(o).forEach(function(k){ if(n==='__cloudInsightByShop'&&k==='forKey') return; var v=o[k]; if(typeof v!=='function'){ table.push({物件:n,屬性:k,處置:'保留（非函式）'}); return; } if(READ_OK.indexOf(k)>=0){ table.push({物件:n,屬性:k,處置:'放行（讀取）'}); return; } o[k]=makeNoop(n+'.'+k); table.push({物件:n,屬性:k,處置:'🛑 已 no-op'}); }); });
  if (!Array.isArray(byShop.shops)) throw new Error('[TEST_NOWRITE] __cloudInsightByShop.shops 不再是陣列。');
  var noopCount = table.filter(function(b){ return b.處置.indexOf('no-op')>=0; }).length;
  window.__TEST_NOWRITE_ON = true;
  console.log('%c🔴 TEST_NOWRITE v2 已啟用：'+names.length+' 個物件 / '+noopCount+' 個寫入方法已停用','color:#fff;background:#ef4444;font-size:15px;font-weight:700;padding:4px 10px');
  console.log('%c掃到的物件：'+names.join(', '),'color:#ef4444;font-weight:700');
  console.table(table);
})();
/* ===== TEST_NOWRITE v2 結束 ===== */
```

**測完拆除順序**：先關分頁 → 再停 server → 最後才刪程式碼。順序反了會出現「檔案沒保護但瀏覽器記憶體還是舊的」。

commit 時只 `git add` 實際改動的檔案，**不要 `git add -A`**，避免夾帶測試碼。

### 版本號 15 處（用 `bump-version.js`，不要手動改）

改任何檔案（**含 CSS**）都要 bump，漏掉同事會拿到舊版。共 **15 處**：

- `index.html` 的 `<meta name="app-version">`（1）
- `index.html` 的 `<script src="js/main.js?v=">`（1）
- `index.html` 的 **3 個 CSS `<link ... ?v=">`**（main.css / profit.css / daily-adjustments.css）← 早期漏列的就是這 3 個；**動到 CSS 一定要一起 bump，否則 `version.js` 只清 Cache API、碰不到瀏覽器 HTTP 磁碟快取，CSS 餵回舊的（v226→227 血淚）**
- `js/main.js` 裡 10 個 import 的 `?v=`（10）

**用 repo 根目錄的 `node bump-version.js <新版號>` 一鍵改全部 15 處＋自動 grep 驗證，不要手動逐一改**（`--check` 可先看現值/一致性）。

不要憑記憶抓號碼，`--check` 或搜 `app-version` 看現況；新號要比檔案裡的、也比 `main` 上的都大（main 會回退，不保證遞增）。

> ⚠ 版號 bump 的**時機**依 CLAUDE.md 版號規則：動到 runtime（js/css）的改動走「本機原子合併」——feature 分支不 bump，合併進 `main` 後才在 main 上跑 `bump-version.js`＋commit＋push（merge 與 bump 同一次 push，零部署空窗）。

---

## 0.5 🚨 部署後第一件事（阻斷性驗收，在任何真實資料進去之前完成）

PChome 的 `ec_pchome_recon` / `ec_pchome_products` 是 **app/profit 泛用欄位**（非獨立 collection）。本機（TEST_NOWRITE）能驗到守衛擋雲端覆蓋、dirty 持久化、待推數、清除「接線」——但**寫入被 no-op 攔截，「真的寫進 Firestore」和「別人讀得到」本機驗不到**。所以上正式站後，**先跑完這 7 步、確認第 6/7 步過，才能開始用**：

1. 開正式站（**無 guard**），確認 Console **沒有** TEST_NOWRITE 紅字（有紅字＝防護碼沒刪乾淨，停手）。
2. 傳一次 PChome 對帳明細。
3. 確認**全域同步鈕**的待推數 **+1**。
4. 按同步。
5. 確認 `localStorage['ec_pchome_dirty']` 清空、待推數回 **0**。
6. 🔴 **確認 Firestore 的 `app/profit` 文件真的出現 `ec_pchome_recon` 欄位。**
7. 🔴 **換一台裝置（或無痕視窗）登入，確認讀得到剛才那筆。**

**第 6 步若沒出現欄位 → 停下來、不要繼續用。** 那代表資料只在本機、畫面看起來正常但實際沒同步（清快取／換機就消失，PChome 後台只留一年帳務）。前 5 步本機都驗過了，只有 6/7 是本機驗不到、非跑不可的。

### 📌 同一天一起做的待辦：確認「列印對帳單」檔案格式

**2026/09/26 開放對帳後**，在對帳單頁面按一次「列印對帳單」（教學 p.9、p.10），確認下載的檔案格式（PDF / Excel / 其他），再評估要不要加「檔案上傳解析」路徑。
- 對帳要 26 號才開放，在那之前按不了這顆鈕，所以檔案格式先不管。
- **貼上解析（月對帳的對帳單數字區）不受影響、是獨立可用的路徑**——不管有沒有檔案都用得到，已經做好了。這條只是評估「能不能連複製貼上都省掉」。

---

## 1. PChome 是什麼模型

**供貨商模式，不是平台抽成模式。** PChome 向我們買斷、再自行定價賣出，對帳單上寫「貴公司應開發票金額」——發票是我們開給 PChome 的。

結構上**與 momo 甲配同構**：

| | momo 甲配 | PChome 一般轉單 |
|---|---|---|
| 營收基準 | 未稅進價 × 對帳數量 | 未稅供貨價 × 數量 |
| 平台抽成 | 無 | 無 |
| 倉租 | 無 | 無 |
| 費用來源 | 對帳單實際金額 | 對帳單實際金額 |

**→ 實作時先讀甲配那段的既有程式碼，沿用同一套框架，不要另起爐灶。**

合約雖載明「每筆訂單 3% 手續費 + 類別抽成」，但**兩期對帳明細 16 個項目全部展開，沒有任何抽成欄位**。供貨模式的平台獲利內含在售價與供貨價的價差裡，不另外抽。**不要為抽成寫任何計算邏輯。**

目前**只做一般轉單，不做寄倉**。寄倉相關的 6 個費用項（包材費、倉儲費、倉庫作業處理費、進貨退出運費、安裝費、分攤運費(寄倉商品)）不會有值。

---

## 2. 資料來源：對帳明細匯出檔

### 檔案格式陷阱

副檔名是 `.csv`，但實際上：

- **編碼：UTF-16LE**（含 BOM）
- **分隔符：Tab**，不是逗號
- **一個檔案含 16 個區段**，每段自己一組標題列 → 標準 CSV parser 讀不了，必須先做區段切分

### 區段結構

每段格式固定：

```
列帳日期區間:\t"2026/08/26 ~ 2026/09/25"
對帳項目:\t"訂單貨款明細 - 一般轉單"
總額:\t="5973"
<標題列>
<資料列...>
<空行>
<空行>
```

16 個區段名稱（依序）：

```
訂單貨款明細 - 一般轉單          ← 營收主來源
訂單貨款明細 - 寄倉訂單
退貨貨款明細 - 一般轉單          ← 退貨
退貨貨款明細 - 寄倉訂單
退貨貨款明細 - 寄倉客退商品(退回廠商)
其他應付
行銷獎勵金(貴公司已開折讓單)
專案獎勵金(貴公司已開折讓單)
退貨物流費
罰金                              ← 有 SKU 明細，可歸屬
分攤運費(寄倉商品)
分攤運費(轉單超取)
行銷獎勵金(PChome開發票)
進貨退出運費(寄倉商品)
分攤安裝費(寄倉商品)
專案獎勵金(PChome開發票)
```

### ⚠ CSV 16 段 → 對帳單欄位分組（不是一對一）

CSV 有 16 段，但對帳單只有 **(A)(B)(C)(D)** 四組。**不要用「段序≠0」之類反向排除法判費用**——那會把退貨貨款（屬 A）、折讓單（屬 B/C）誤當成費用。用**名稱**對應（程式：`PCHOME_SEG_GROUP`）：

| 段 | 對帳項目 | 對帳單欄位 |
|---|---|---|
| 0 | 訂單貨款明細 - 一般轉單 | (A1) |
| 1 | 訂單貨款明細 - 寄倉訂單 | (A1) |
| 2 | 退貨貨款明細 - 一般轉單 | (A2) |
| 3 | 退貨貨款明細 - 寄倉訂單 | (A2) |
| 4 | 退貨貨款明細 - 寄倉客退商品(退回廠商) | (A2) |
| 5 | 其他應付 | (A3) |
| 6 | 行銷獎勵金(貴公司已開折讓單) | (B1) |
| 7 | 專案獎勵金(貴公司已開折讓單) | (C1) |
| 8 | 退貨物流費 | (D1) |
| 9 | 罰金 | (D2) |
| 10 | 分攤運費(寄倉商品) | (D3) |
| 11 | 分攤運費(轉單超取) | (D3) |
| 12 | 行銷獎勵金(PChome開發票) | (D4) |
| 13 | 進貨退出運費(寄倉商品) | (D9) |
| 14 | 分攤安裝費(寄倉商品) | (D10) |
| 15 | 專案獎勵金(PChome開發票) | (D12) |

- **(A) 貨款 = (A1)+(A2)+(A3)**（段 0–5）；**(B)/(C) = 折讓單**（段 6/7）；**(D) 費用 = 段 8–15**。
- **費用卡與 (D) 差額計算只吃 (D) 組（段 8–15）。** 費用只有這 8 段。
- 🔴 **以下 (D) 子項在 CSV「沒有對應區段」**：(D5) 簡訊費、(D6) 包材費、(D7) 倉儲費、(D8) 倉庫作業處理費、(D11) 產品責任險、(D13) 廣告費、(D14) 加值服務、(D15) 第三方廣告代收款。**這就是必須手動輸入 (D) 的必要性來源**——CSV 明細加總永遠湊不齊 (D)，差額（(D) − CSV 有明細費用）就是這些無區段項的合計，計入未分攤費用。
- 安全網：CSV 出現對照表未知的段且有值時，費用卡跳紅字警告、不靜默吞掉（PChome 新增段或改名時要補進 `PCHOME_SEG_GROUP`）。

### Excel 公式跳脫

多個欄位被包成 Excel 公式形式，解析時要剝掉：

| 欄位 | 原始值 | 實際值 |
|---|---|---|
| 訂單編號-序號 | `="20260827201081-01"` | `20260827201081-01` |
| 廠商料號 | `="F204-10"` | `F204-10` |
| 總額 | `="5973"` | `5973` |
| 出貨單號 | `"""677172789101"""` | `677172789101` |

**出貨單號是 12 位純數字，必須以字串讀取**，轉 number 會掉精度。

### 一般轉單欄位

```
訂單編號-序號  商品編號  商品名稱  規格名稱  數量  單位成本
成本小計  團購折扣  應付金額  單號確認日  轉單日期
出貨單號  單號回填日  廠商料號
```

### 罰金欄位

```
訂單編號-序號  商品編號  商品名稱  規格名稱  數量  轉單日期
延遲起算日  出貨日  罰金天數  入帳日期  罰款
```

---

## 3. 口徑定義

### 營收

**營收（未稅）= 應付金額 ÷ 1.05**

`應付金額` 是含稅（對帳單 (A) 欄位明寫「應開發票金額(含稅)」）。`單位成本` 是 PChome 的進貨價，也就是我們的供貨價——**不是我們的成本**。

`成本小計 = 單位成本 × 數量`，`應付金額 = 成本小計 − 團購折扣`。

### 成本

**沿用甲配做法：建檔時**以 `廠商料號` 查 `ec_momo_cost_by_origin`（查不到的手動填），把查到的值**寫進 `product.cost` 凍結**。**計算時直接讀 `product.cost`，不做 live 查詢。**

⚠️ 為什麼建檔時查、不 live 查：PChome 成本要人工乘好入數（見下），live 查會讓「成本表改了但入數沒重乘」悄悄傳播到歷史報表；建檔時寫死，歷史數字凍結。（甲配實作同此：`momoAddOriginLookup` 在建檔輸入原廠編號時查表寫入 `product.cost`，`momoAggregatePeriods` 計算時只讀 `product.cost`。）

⚠️ **成本表存的就是「PChome 這一件的成本」，程式不做任何入數換算。** 多入裝商品（例：`水槽過濾網-200入` 對應成本表 100 入 × 2）的倍率在填表時就乘好，程式直接取用。

**不要寫倍率欄位、不要寫換算邏輯。**

唯一的防護是顯示層：成本欄位旁顯示供貨價/成本比值，異常高的比值（8 倍以上）代表可能忘了乘，**只標示不阻擋**。

### 費用

**全部是對帳單實際金額，沒有任何費率需要推算。** 不要建費率模型。

費用同樣是含稅，計算淨利時除以 1.05。

歸屬規則：

- **罰金**——明細有 `商品編號`，歸屬到 SKU
- **其餘費用項**——沒有 SKU 明細，列為「未分攤費用」，只進合計不進個別商品

**不要照營收比例分攤罰金。** 罰金是特定商品的營運事故，攤到其他商品會讓淨利率失真（這與 MO+ 的 feeB 按同單營收比例分攤不同，那是真正的共同費用）。

因為有未分攤費用，**各列淨利加總不等於合計**，要有獨立一列讓兩邊對得起來。

---

## 4. 匯入規則

### 快照覆蓋，不能累加

**這是最容易出錯的一條。**

同一個帳務週期會被重複匯出，內容不同：

| 匯出時間 | 列帳日期區間 | 資料列 | 貨款 | 罰金 |
|---|---|---|---|---|
| 2026/09/08 | 2026/08/26 ~ 09/25 | 2 | 365 | 115 |
| 2026/09/16 | 2026/08/26 ~ 09/25 | 21 | 5973 | 0 |

期間完全相同。而且**罰金申訴成功後會被改寫成 0**，代表歷史資料是可變的。

**→ 以「帳務月」為主鍵（＝列帳日期區間起始日正規化），後匯入完整覆蓋先前。絕對不能 append。** 重傳一次就翻倍是這個功能最可能的 bug。

⚠️ **主鍵用「帳務月」不是完整區間字串**（教學修正）：後台「本期」結束日會隨當天變動（教學截圖 `本期(2025/08/26~2025/09/02)`），但 CSV 匯出的區間字串是完整月區間（實測兩份都是 `2026/08/26 ~ 2026/09/25`）。為防未來變動，主鍵用**起始日正規化成帳務月**（週期 26→25，起始月 M → 帳務月 M+1，如 `2026/08/26` → `2026-09` 期）。起始日不同才算不同期，不要用完整字串比對。

### 期別歸屬

帳務週期是 **08/26 ~ 09/25**，不是自然月。每列**用 `單號確認日` 歸到自然月**（教學修正，見下），與甲乙配的期別維度一致，**不需要為 PChome 開獨立期間軸**。

⚠️ **歸期依「單號確認日」，不是「轉單日期」**（教學 p.7 修正、會算錯期）：教學原文「依單號確認日落在【列帳日期區間】內之訂單，列入該期帳務」「廠商完成出貨回填後…確認出貨單號無誤時即產生單號確認日」。實測落差常達兩天（`20260827201081-01` 轉單 08/27／確認 08/29；`20260913416211-02` 轉單 09/13／確認 09/15），期間內看不出、但月底交界（9/24 轉單、9/26 確認）會歸錯期。**calc 與月對帳的歸期一律讀 `單號確認日`；`轉單日期` 只在訂單明細顯示、不作歸期依據。**

本期未結束時不要算成長率，標示「本期未完」。

### 訂單分組

`訂單編號-序號` 的格式是 `<14位訂單號>-<2位序號>`。

**group key = 最後一個連字號之前的部分。**

序號會跳號，這是正常的——PChome 一張訂單可混多家廠商，我們只拿得到自己那幾序。實際樣本裡有 `-03`、`-10`、`-06`、`-04` 單獨出現的情形，**不要當成資料缺漏去補**。

同單多序範例：`20260909326564-01/-02/-03`（腰帶三色），出貨單號同為 `677172815185`。

### 料號對應

`廠商料號` 是**規格層級**的鍵，可直接查成本表，不需中間對照表。

PChome `商品編號` 的尾碼對應規格：

```
DIAECY-A900KE6PT-003 → D27-02  (軍綠)
DIAECY-A900KE6PT-004 → D27-01  (黑色)
DIAECY-A900KE6PT-005 → D27-11  (藏青)
```

`單位成本` 在全期內每個料號都一致，沒有逐單浮動。

---

## 5. UI 規格

框架照甲配。區塊頂端：

- 標題 `PChome` + 模式標籤（`一般轉單` 啟用 / `寄倉` 未啟用）
- 期別下拉
- 副標顯示帳務週期與歸期方式

**KPI 卡（4 張）**：營收（未稅）｜銷量｜加權淨利率｜費用

**版面完全照 momo 甲配總表**（2026-09 拍板；早期「品號｜商品兩欄」規格作廢，momo 優先）：沿用 momo 既有 class，唯一品牌色差異＝`.pf-pchome` 的 #E60012（只用在 pills／按鈕，表格/KPI 走 momo 中性色）。

**商品明細表欄位**（單一 sticky 首欄，比照 momo `.mm-name-clip`+`.mm-sub-line`）：

```
商品(sticky:商品名+商品編號·料號)  成本  供貨價  售價  營收  銷量  費用  淨利  淨利率
```

- 首欄 `.mm-sticky-col`：商品名（`.mm-name-clip` 省略號、hover 全名）＋第二行 `.mm-sub-line` 放**兩個識別碼**「商品編號 · 料號」（比照 momo「品號 · 原廠」：momo 品號→PChome 商品編號、momo 原廠→廠商料號）。**不再拆成品號/商品兩欄。** 商品編號有兩來源（對帳明細／上架清單），**不一致→標紅並兩邊都列（sub-line ⚠＋上方 `.mm-banner-err`），不靜默挑一個。**
- **成本｜供貨價｜售價 三欄相鄰、放在銷量之前**（比照 momo 成本｜進價｜售價）。稅別照 momo 口徑（查證：momo 進價=未稅、售價=含稅）：
  - **供貨價（未稅）**＝對帳明細「單位成本」(PChome 向我方進貨單價，含稅) ÷1.05。與營收同為未稅、同基準，不會與營收混稅別。
  - **售價（含稅・網路價）**＝上架清單「售價(網路價)」(消費者實付、不進帳僅參考)。**含稅**——與同列未稅的營收/供貨價稅別不同，欄名／提示要標清楚（比照 momo salePrice「（含稅）」）。上架清單無此料號→顯「—」（比照缺成本，不顯 0/空白）。
  - 🚫 **不加「PChome 毛利率」等衍生欄**：那是 PChome 的毛利、不是我方淨利，放我方淨利表只會混淆。要加另議。
- **多入標記已移除**（總表不再偵測商品名數量字樣）：成本表存的就是這一件的成本、入數在填表時已乘好，畫面不需再提醒。⚠ 商品同步分頁目前**也沒有**多入提醒（本專案從來只有總表有過）；若日後要在商品同步（填成本處）加「記得乘入數」提醒，是新增項、另議。
- **已知差異：PChome 總表不支援欄寬拖曳（col-resize）。** momo 的 `momoColResizeDrag` 綁死 `momoDisplayCols`（momo 專屬欄），無法直接沿用；另寫是行為功能、非樣式。視覺上以 `<colgroup>` 固定欄寬達成一致。之後有人問起才不會以為漏做。
- 表格容器 `.tscroll`＋`<table class="mm-ptbl" table-layout:fixed>`＋`<colgroup>` 固定欄寬；表頭 `.mm-th`（sticky、可排序、`?` 提示、排序箭頭）；數字欄靠右、`.tscroll td` 15px。
- **金額格式照 momo `momoMoney`：`$` 前綴、四捨五入到整數、負數 `-$X`**（跨平台一致）。🔴 **合計必須由「未四捨五入的完整精度值」加總後才 round**，不可把各列已四捨五入的數字相加；逐列顯示值相加與合計差 ±1 元屬正常、可接受。淨利率照 `momoPct` 1 位小數。
- 費用欄無值顯示 `—`（`#c7cad1`，同 momo 缺值慣例）；正負值用 `.td-pos`#10b981 / 紅 #ef4444。
- 表尾：「未分攤費用」列（比 `.tr-total` 輕的小計列，bg #fafbfc 灰字）＋「合計」列（`.tr-total`）。
- KPI 4 卡 `.mm-kpi`：`vs 上期` delta（`.mm-kpi-d`）**只在「上一期有資料且本期已結束」時顯示**（本期未完不顯示，同「本期未完不算成長率」）。
- PChome 特有元素一律用 momo 語彙：缺成本→`.mm-banner-err` 紅橫幅；本期未完→`.mm-status` chip（放期別選擇器旁，琥珀）；簡訊費推算→`.mm-banner-warn`。**不自創視覺樣式。**

**對帳單數字：整塊貼上解析（已實作，主要輸入方式）**

CSV 明細不完整（(D5)簡訊費等 8 項無區段，見 §2 對照表），但**使用者已經有對帳單頁面、不該要人手打**。月對帳的「對帳單數字」區塊做的是**貼上解析**：

- 一個 textarea + 「解析」鈕：把對帳單頁面表格**整塊複製**貼上 → 解析出 (A)(B)(C)(D)(F) 主值 **＋每個 (D1)~(D15) 子項**（比手打多很多，費用卡能顯示完整 (D) 組，不再只有 CSV 那幾段）。
- 解析靠「每列開頭代號 `(X)` ＋該列第一個數字 token」抓值，**不靠固定欄位位置**（(A1) 那種前面多 descriptor 也吃得到）。`---`/`—` 視為 0；千分位逗號去掉；一列既無數字也無破折號＝沒解析到、**留空不猜**。
- 也解析「本期(YYYY/MM/DD~YYYY/MM/DD)」→ 正規化成帳務月，**與 CSV 帳務月比對，不同就警告**（防貼錯期別的對帳單）。
- **解析完先顯示預覽（期別檢查＋主值＋(D)子項表）讓使用者確認，不直接寫入**；按「儲存」才落地（跟帳務月一起 snapshot 覆蓋）。
- (A)-(F) 五欄保留為**「解析後可修正」**（不是主要輸入），解析會預填、使用者可改；純手動改不重貼時保留既有 (D) 明細/期別。

**費用明細卡**：優先顯示**貼上解析的完整 (D) 組**（含 CSV 無段的簡訊費等）；沒貼過才退回只顯示 CSV 有明細的 (D) 段。加對帳單 (F) 實付金額做勾稽錨點。

驗算列：(F)=(A)−(B)−(C)−(D)；(A) vs CSV 貨款差額（差額多半是重傳 CSV 後 (A) 沒更新，文案要點明「不是 CSV 錯」）；(D)−CSV有明細費用 = 未分攤缺費（＝CSV 無區段的 (D) 子項合計，計入未分攤，總表淨利用）。

對帳單公式：

```
(A) = (A1) + (A2) + (A3)
(D) = (D1) + ... + (D15)
(F) = (A) - (B) - (C) - (D)
```

⚠️ **待 09/26 開放對帳後**：若「列印對帳單」能下載檔案（PDF/Excel），評估加「檔案上傳解析」省掉複製貼上（見 §0.5 待辦）。貼上解析是獨立可用路徑、不受影響。

**真實貼上結構（2026/09/18、09/08 兩份 fixture，從後台實際複製）——解析器有對應處理：**

- 期別行：`帳務週期：「本期(2026/08/26~2026/09/25)」`（全形引號、`~` 前後**無空格**；CSV 版本是 `2026/08/26 ~ 2026/09/25` **有空格**——兩種都要吃）。
- 多數列單行：`(X)⇥標籤⇥值⇥…`，值取代號後第一個數字/`---`。
- **(A1)/(A2) 是多行列**：代號行只有標籤、值在後面數行——先所有子項標籤、再依序對應的值、最後一堆按鈕文字（「天天」「檢查或調整單價」「檢查」…無阿拉伯數字＝雜訊）。例：
  ```
  (A1)⇥訂單貨款⇥
  一般轉單
  寄倉訂單
  6,309          ← 一般轉單的值
  ---            ← 寄倉訂單的值（0）
  天天 / 檢查或調整單價 …   ← 雜訊，丟掉
  ```
  → 解析成 (A1)=6,309，子項 {一般轉單:6309, 寄倉訂單:0}。**(A1) 的轉單/寄倉拆分＝日後兩賣場營收拆分依據，現在先存（`對帳單.子項`）。**
- `(D2)⇥罰金⇥---⇥天天檢查或申訴罰金⇥---`：中間夾雜「天天檢查或申訴罰金」文字，但無阿拉伯數字 → 取第一個 `---`=0，不受干擾。
- `(F)⇥PChome實際支付金額(含稅)公式⇥6,286`：標籤尾巴多「公式」兩字，只是標籤、不影響取值。
- 驗過的解析結果：fx1（09/18）(A)=6309/(D)=23/(D5)簡訊費=23/(F)=6286，F=A−B−C−D ✓；fx2（09/08）(A)=365/(D)=122/(D2)罰金=115/(D5)簡訊費=7/(F)=243 ✓。
- ⚠ 09/26 真的複製新對帳單時，若欄位順序/換行跟這兩份不同，解析可能要微調——這兩份是目前唯一的真實依據。

**費用段判定用名稱對照表（§2），不用「≠0」反向排除**——退貨貨款屬 (A)、折讓單屬 (B)/(C)，只有 (D) 組（段 8–15）才是費用。CSV 出現對照表未知的段且有值 → 費用卡跳警告、別靜默當費用。

---

## 6. 規則引擎

- **賠錢判斷**（規則 1）直接接
- **倉租相關規則**（規則 6-7）不適用，PChome 無倉租
- **罰金不另立規則**：罰金就是普通費用項，依費用項通則處理——有值進合計、有 SKU 明細（商品編號）就歸屬到該 SKU、沒值不顯示。不為它寫專屬規則或旗標。

---

## 7. 風險結構（影響優先級判斷）

這條線的獲利特性與其他通路不同：

| | |
|---|---|
| 毛利率 | 75~83%（實測兩支） |
| 平台費率 | 0% |
| 主要風險 | **出貨延遲罰金** |

罰金 115 元一次 ≈ 吃掉該商品 93.6% 的毛利。罰金明細有 `罰金天數` 欄位，代表**按天累計**。一筆漏單的總損失（罰金 + 錯失毛利）約 238 元，相當於兩件的獲利。

**→ PChome 這條線不需要在費率、定價上著墨，該投資的是出貨狀態的可見度。** 之後若要加功能，「有罰金的訂單」比「低毛利商品」更值得做。

---

## 8. 測試樣本

09/16 匯出檔：21 列、18 張訂單、16 個料號、27 件，貨款 5973（未稅 5688.57），D 項全為 0。

可用來驗證的邊界情況：

- 同單多序（`20260909326564-01/-02/-03`）
- 序號跳號（`-10`、`-06`）
- 單列多件（數量 2、3）
- 同商品不同規格（`E150-20` / `E150-35`，`F421-01` / `F421-12`）
- 多入裝商品（`F204-10`、`D46-03/04`、`F332-10`）

09/08 匯出檔可用來驗證**快照覆蓋**：先匯 09/16 再匯 09/08，貨款應變回 365 而不是 6338。

---

## 9. 還未驗證的部分

以下都**沒有實際樣本**，不要預先實作：

- **退貨**——兩期皆為 0。退貨明細欄位含 `入帳日期`，若與原訂單 `轉單日期` 錯開，歸期邏輯需另議
- **分攤運費(轉單超取)**——含 `溫層` 欄位，可能有冷藏/冷凍差別費率
- **廣告費 / 行銷獎勵金 / 專案獎勵金**——折讓單性質，與貨款是不同的會計流向
- **罰金計費規則**——單一樣本反推疑似「零售價 × 50%」（229 × 50% = 114.5 ≈ 115），**未經證實，不要寫進程式碼**

### 簡訊費推算規則（已實作為「推算」，非事實）

從 (D5) 簡訊費明細頁（對帳單每個 (D) 項旁的「檢查」連結）推出：簡訊分六類（新增訂單／取消訂單／出貨通知／對帳通知／換貨通知／借貨通知）。實測本期只有「新增訂單」＋「取消訂單」。

**規則：新增訂單簡訊 ＝ 不重複訂單數（按「轉單日期」歸期、不是明細列數）× 1 元（含稅）。**

- **驗證**（用 09/16 CSV 逐日對，完全吻合）：09/07 3 單→3、09/09 4 列但只 2 單（326564/426991）→2、09/10 3 單→3、09/12 2 單→2…；程式 `grp(訂單編號-序號)`（最後連字號前）去重後與此一致。09/16 CSV 共 **18 張不重複訂單**；累計到 09/17＝23，與 09/18 對帳單 (D5)=23 吻合。
- **實作**：`pchomeProfitCalc` 回 `簡訊費推算{訂單數,含稅,未稅}`；總表底部「簡訊費（推算）」諮詢卡（紫色虛線＋「推算」標，與實際費用視覺區分）。**不計入合計、不落地當事實**（顯示層估計）。
- **三個但書（UI 已標）**：
  1. **單價 1 元未經 PChome 公告**，只有單一樣本反推佐證。
  2. **取消訂單簡訊算不出來**（取消的訂單不在對帳明細裡）→ 推算**偏低**（本期 31−29＝2 元缺口）。
  3. **有實際 (D5) 時一律以實際為準**（貼上解析取得），推算退居參考並顯示差額（差額＝取消訂單簡訊＋單價假設誤差）。
- ⚠ **PChome 若改價或新增簡訊類型，這條規則要重驗。**

### 費用雙狀態（即時／已對帳）——為什麼轉單模式的即時推算可接受

**問題**：對帳單每月 26 號才開放，不可能等對帳單才知道當月狀況。**先例**：momo 甲配在對帳單未到時本來就用 `estFeeRate`（近 3 月均費率或 6.8%）暫估費用（`momoAggregatePeriods` L11698-11701）＋狀態晶片「⚠ 未對帳」＋橫幅——「期中用估計值」在本專案早有先例。

**PChome 的雙狀態**（`pchomeProfitCalc` 回 `feeState`）：

| 狀態 | 觸發 | 費用 | 標示 |
|---|---|---|---|
| **即時** | 對帳單未貼上 | CSV (D) 組實際 ＋ **簡訊費推算**（計入合計、標推算） | 期別旁 `.mm-status no`「未對帳」；費用 KPI「即時・含簡訊推算 $N」；`.mm-banner-warn` 說明 |
| **已對帳** | 貼上對帳單 (D) | 對帳單 (D)（權威） | `.mm-status ok`「已對帳」；banner 顯示與即時值差額（＝取消訂單簡訊＋單價假設誤差）|

**為什麼即時值可接受（誤差個位數元）**：轉單模式**0% 平台抽成**，(D) 費用裡罰金／退貨物流費／分攤運費**CSV 本來就有實際區段**；寄倉項不適用；廣告費使用者自知。**唯一 CSV 拿不到的只有簡訊費**，已驗證到只差取消訂單那筆（本期 31−29＝2 元）。所以即時值＝CSV 實際＋簡訊推算，遠比顯示 0 準。

🔴 **刻意不做 `estFeeRate` 那種 %比例暫估**：轉單模式沒有比例費，加了只會變成沒人知道要填什麼的空欄位。推算標示一律用 `.mm-banner-warn`，不自創樣式。

---

## 10. 專案既有慣例（照做）

- 開工前 `git pull`（main 很活躍，會回退）
- 樣式寫進 `css/`，不用 inline style
- 純手寫 HTML + CSS + 原生 JS，**沒有建置流程，不能用需要編譯的語法**
- 跨檔呼叫走 `window`，模組結尾要 `Object.assign(window, {…})` 掛回去——**忘了掛，按鈕會 ReferenceError 靜默失效**
- 一次只做一塊，做完驗過再做下一塊
- 改完要在瀏覽器實際點過才 commit
- push 前先 `git fetch` + `git log --oneline HEAD..origin/main` 防撞，有輸出先 merge
- 用 merge 不用 rebase
- push 完直接開 PR 直接合併，中間不要跑去做別的事

### PChome 專屬（2026-09-16 起，一律適用）

- **一律先在 localhost 開給人測、驗過才 push 才上正式站。** 不要寫完直接 push、不要「小改動所以直接上」。每完成一塊就把 server 起著、給網址 + 要測什麼，對方實際點過再往下。
- 交 server 前，**自己先看一次 Console 確認 TEST_NOWRITE 生效**（見 §0）——localhost 點到的任何東西都不能寫進正式 Firestore。
- **測完拆除順序（勿顛倒）**：先關分頁 → 再停 server → 最後才刪防護碼。反了會「檔案沒保護但瀏覽器記憶體還是舊的」，重整就寫正式庫。
- 刪完 `git diff js/firebase.js` 應為空；commit 只 `git add` 實際改動的檔案，**不要 `git add -A`**（免夾帶測試碼）。
